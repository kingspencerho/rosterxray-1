# Generated by build.sh. The PDF is kept because it is the deliverable people
# are actually sent; the HTML is 1.3MB of inlined fonts and rebuilds in one
# command, so it is not worth versioning.
report.html
artifact.html
